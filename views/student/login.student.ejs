<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/form.css">
    <link rel="stylesheet" href="/css/footer.css">
</head>
<body class="container">
    
    <%- include('../includes/navigation.ejs') %>

    <div class="hero-1">
        <h3 class="heading">Find Result</h3>

        <% if(typeof alert != 'undefined') { %>
            <div class="error-wrap">
                <div class="alert alert-warning" role="alert">
                    <% if(alert.length === 2) { %>
                        Invalid Credentials
                    <% } else { %>
                        <%= alert[0].msg %>
                    <% } %>
                </div>
            </div>
        <% } %>

        <form action="/student" method="POST" class="row g-3">
            <div class="col-md-12">
                <label for="roll_no" class="col-sm-12 col-form-label">Roll No*</label>
                <input type="text" class="form-control" name="roll_no">
            </div>
            <div class="col-md-12">
                <label for="dob" class="col-sm-12 col-form-label">Date of Birth*</label>
                <input type="date" class="form-control" name="dob">
            </div>
            <div class="col-md-12 actions">
                <button type="submit" class="btn btn-primary btn-login">Search</button>
                <button type="reset" class="btn btn-danger btn-reset">Clear</button>
            </div>
        </form>
    </div>

    <%- include('../includes/footer.ejs') %>

<%- include('../includes/end.ejs') %>